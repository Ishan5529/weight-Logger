# weight-Logger
Simple web-app to log daily weight and visualize progress using ChartJS !!

# NOTE:
The application uses __localStorage__ for keeping track of the records. So, progress will be lost, if browser cache is cleared.
